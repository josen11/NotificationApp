{
    "Logging": {
        "LogLevel": {
            "Default": "Information",
            "Microsoft.AspNetCore": "Warning"
        }
    },
    "AllowedHosts": "*",
    "ConnectionStrings": {
        "DefaultConnection": "Server=.;Database=LocalServiceCenter;Trusted_Connection=True;TrustServerCertificate=True;" // ← leave blank, we’ll fill via env var
    },
    "JwtSettings": {
        "Issuer": "", // ← same here
        "Audience": "",
        "SecretKey": ""
    }
}
